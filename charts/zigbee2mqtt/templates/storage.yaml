---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: zigbee2mqtt-zfs-sc
  namespace: {{ .Release.Namespace }}
parameters:
  recordsize: "128k"
  compression: "off"
  dedup: "off"
  fstype: "zfs"
  poolname: {{ .Values.zfs.poolName }}
reclaimPolicy: Retain
provisioner: zfs.csi.openebs.io
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: zigbee2mqtt-pv-data
spec:
  capacity:
    storage: {{ .Values.zigbee2mqtt.volume.data.size }}
  accessModes:
    - ReadWriteOnce
  storageClassName: zigbee2mqtt-zfs-sc
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: zfs.csi.openebs.io
    volumeHandle: zigbee2mqtt-pv-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zigbee2mqtt-pvc-data
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: zigbee2mqtt-label
spec:
  volumeName: zigbee2mqtt-pv-data
  accessModes:
    - ReadWriteOnce
  storageClassName: zigbee2mqtt-zfs-sc
  resources:
    requests:
      storage: {{ .Values.zigbee2mqtt.volume.data.size }}
