---
apiVersion: v1
kind: Service
metadata:
  name: homeassistant-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app.kubernetes.io/name: homeassistant-label
  ports:
  - name: homeassistant
    protocol: TCP
    port: {{ .Values.homeassistant.ingress.servicePort }}
    targetPort: ha-web
