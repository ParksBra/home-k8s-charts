apiVersion: v1
kind: ConfigMap
metadata:
  name: homeassistant-configuration
  namespace: {{ .Release.Namespace }}
data:
  configuration.yaml: |
    {{ .Values.homeassistant.config.configuration | nindent 4 }}
